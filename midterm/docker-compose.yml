version: '3'
services:
    web:
        build: .
        container_name: rahul-midterm
        ports:
            - 5020:5000
        volumes:
            - ./data/animals.json:/animals.json
    redis:
        image: redis:latest
        ports:
            - 6400:6379
        volumes:
            - ./config/redis.conf:/redis.conf
            - ./data/animals.json:/animals.json
        command: [ "redis-server", "/redis.conf" ]


